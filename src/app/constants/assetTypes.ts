// TODO: These should be on separate lines for readability and maintainability. That way
// if a new item is added we can see who added it and when by using source control.
export const AssetTypes = ["Desktop", "Laptop", "Display", "Phone", "External Hard Drive", "Other"];